## 1. Clear the path from the report to the projection

- [ ] 1.1 Narrow the `pyproject.toml` import contract "The deterministic report path never reaches the judge" from `agentlens.judge` to `agentlens.judge.cli_backend` and `agentlens.judge.invocation`, keeping `core.ingest_run` in `source_modules` and indirect imports disallowed, and rename it for what it now guards. Assert it fails before trusting it to pass: temporarily import `ClaudeCliJudge` into `core/report.py`, confirm `lint-imports` rejects it, then remove the import. Verify `make check`'s `lint-imports` leg passes while `core.report` may import `judge.prompt`.
- [ ] 1.2 Record the decision as an ADR under `docs/adr/`, and index it in `docs/ARCHITECTURE.md`: why the report path may render a judge prompt but never construct a backend, what the injected-seam rule contributes that no import contract can, and that `judge_input_hash` is independent of `rubric_version` because `render_prompt` carries no rubric text. Verify the ADR is reachable from `docs/ARCHITECTURE.md`.
- [ ] 1.3 Parametrize the rubric version through `core/spawn_scoring.py`'s scoring inputs and `SpawnScoringPreview` so a verdict lookup can name a cohort other than the current constant, threading `RUBRIC_VERSION` from the existing scoring callers. Cover: the scoring path's reuse and claim behavior unchanged, a lookup under an older rubric version finding a verdict the current constant misses, a lookup under an unknown rubric version finding nothing. Verify with `make quick`.

## 2. Read a window's verdicts in one call

- [ ] 2.1 Add `read_verdicts_for_sessions` to `store/verdicts.py` and its `Store` wrapper, mirroring `read_skill_signals_for_sessions`, returning every verdict held for the given qualified session ids across rubric versions, judge models, and judge-input hashes. Cover: an empty id set returning no rows rather than the whole table, a session with no verdicts contributing nothing rather than a placeholder, a session holding verdicts in several cohorts returning all of them, ids absent from the store, and a first-run store with no verdict rows at all. Verify with `make quick`, and verify `store/reporting.py` is unchanged.

## 3. Keep the parsed source bundles

- [ ] 3.1 Add the qualified-session-id-to-source-bundle map to `PreparedIngestBatch` in `core/ingest_run.py`, and make `core/window_scoring.py`'s `_WindowWorklistBuilder` consume it instead of repeating discover-and-parse. Cover: window scoring's existing outcomes and counts unchanged, the map keyed by qualified session id, and a windowed spawn matching no bundle still failing as it does today. Verify with `make quick` and that `core.ingest_run` still imports nothing from `judge`.

## 4. Resolve the cohort before computing anything modeled

- [ ] 4.1 Add the cohort type and its resolution to `core`: group the window's verdicts into `(rubric_version, judge_model)` cohorts within the filtered scope, then select or refuse. Cover: zero cohorts naming no cohort without error, a sole cohort selected and recorded as implied, a sole cohort named explicitly and recorded as requested, more than one cohort with no selector raising `ConfigError` that names each cohort in selector form with its spawn coverage, a named cohort absent from scope raising `ConfigError` that names what is present, an alias named instead of a concrete judge model landing on that same failure, an agent filter narrowing what counts as ambiguous, and coverage counts never breaking a tie. Verify with `make quick`.
- [ ] 4.2 Add the `--cohort <rubric_version>/<judge_model>` option to `cli/report.py`, split on the first separator, carried on `ReportArgs`. Cover: a valid selector, a selector with no separator, an empty rubric version or judge model, a judge model that itself contains the separator, exit code 2 on a malformed selector, and no store or report artifact written on that failure. Verify with `make quick`.

## 5. Join verdicts to spawns and aggregate

- [ ] 5.1 Join each window spawn to the named cohort on the spawn's current judge-input hash and give every spawn a modeled state. Cover: an unchanged source scoring, a source grown since scoring reporting stale and contributing to no average, a spawn with no cohort verdict reporting unscored with no defaulted score, all three states present in one window with per-state counts available, and at-most-one-score-per-spawn asserted as a property of the four-part verdict key rather than by exercising a deduplicator. Verify with `make quick`.
- [ ] 5.2 Compute modeled agent rollups in `core` over scored spawns for the current and prior windows, gated by `min_sessions_for_trend` from `ResolvedWindow` against the modeled population. Cover: an agent type whose deterministic trend is comparable while its modeled trend is insufficient because the populations differ, both populations meeting the threshold yielding current values, prior values, signed deltas, and a comparable status, an agent type with no scored spawns omitting the rollup rather than zeroing it, and a test asserting the modeled and deterministic gates use the same threshold. Verify with `make quick`.

## 6. Put it in the document and on the terminal

- [ ] 6.1 Extend the report document types and `render/document.py` with the cohort block including how the cohort was selected, the per-spawn modeled block reusing `_build_verdict_row` for its provenance marking, and the modeled rollup; advance `REPORT_SCHEMA_VERSION` to 2. Cover: measured fields and deterministic rollups readable without traversing modeled structures, every version-1 field keeping its meaning and location, evidence and fixes marked untrusted beside locally derived scores, agentlens's own judge spend for the cohort as a currency figure while analyzed token usage stays a count, a window with no verdicts producing a document with no cohort and no modeled rollup, and `test_normal_and_dry_run_documents_are_equal_apart_from_generation_timing` still passing. Verify with `make quick`. Do not touch `render/document.py`'s single-session `SCHEMA_VERSION`.
- [ ] 6.2 Extend `render/summary.py`'s report summary with the cohort and the scored, stale, and unscored counts. Cover: a window with scored spawns, a window with none, and an assertion that no evidence, recommendation, rationale, or fix-target string reaches the summary. Verify with `make quick`.

## 7. Change-level gate

- [ ] 7.1 Run `make check` once for the whole change and resolve anything it reports, then run `graphify update .` so the knowledge graph reflects the new modules and edges.
