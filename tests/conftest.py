"""Fixtures with a real lifecycle to manage, and only those.

A plain constructed value belongs in ``factories.py`` and a test double belongs in
``fakes.py``. A fixture that only returns an object hides which object a test used.
"""
